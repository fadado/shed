########################################################################
# Container Shedfile for hello world example
########################################################################

# Base image
IMAGE="busybox:latest"

# Command to run (a list of words or a string without spaces)
CMD=('/bin/echo' 'Hello,' 'world!')

# Allocate a pseudo-TTY
TTY='true'

# vim:syntax=sh:ai:sw=8:ts=8
